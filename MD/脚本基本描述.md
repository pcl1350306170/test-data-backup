## 修改【批量重命名文件】的脚本
* 具体需求如下
* 1、可视化操作，选取某一个文件夹-保存为配置文件，默认H:\NOVEL\合集\old\HH。
* 2、可以输入文件重命名的的替换规则-保存为配置文件。规则是把字符替换为空，比如“《”“》”“章节”“已完结”“_”“-”的字符替换为空。
* 3、文件名里面的数字，括号和括号里面的内容替换掉
* 4、可以配置下载重试次数-保存为配置文件D:\book\封面
* 5、配置文件引入方式如下：
    * 绝对路径引入配置文件的代码，确保无论脚本在何处执行，都能准确定位到配置文件
    * 通用数据库配置：DB_CONFIG_PATH，都是在运行脚本的父级目录的json文件夹，里面的DB_CONFIG.json
* 7、可视化操作给我加上合适的操作按钮
* 8、记录操作日志“PROCESS_LOG_FILE”


### 配置实例
```python
# 配置与常量
import os
from pathlib import Path

SCRIPT_DIR = Path(os.path.abspath(os.path.dirname(__file__)))
SCRIPT_NAME = "chinese_translator"
CONFIG_DIR = SCRIPT_DIR / "json"
CONFIG_PATH = CONFIG_DIR / f"config_{SCRIPT_NAME}.json"
CONFIG_DIR.mkdir(exist_ok=True)
DB_CONFIG_PATH = (SCRIPT_DIR.parent) / "json" / "DB_CONFIG.json"
PROCESS_LOG_FILE = SCRIPT_DIR / "json" / "logs" / f"log_{SCRIPT_NAME}.log"
```

