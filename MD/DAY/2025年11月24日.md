请为我设计一个适配手机、平板等移动设备的[列举2的N次幂游戏]前端页面，要求如下：
1. 响应式布局：在手机（320px+宽度）、平板（768px+宽度）上自动适配，数字显示区域随屏幕尺寸缩放，按钮、数字块不溢出且触摸区域≥44px×44px（符合移动交互标准）；
2. 核心功能：页面显示一个输入框【最大值1000】，可以输入一个数字【N】，一个点击按钮，点击后，页面弹窗开始显示2的次幂，从2^1开始一直显示到2^N，一行一个值，数字可能很多，需要一直可以显示最新的数字，可以滑动显示之前的数字。js直接算出来的值可能不对，可以直接把结果写js里面，显示的时候直接调用。
3. 交互体验：滑动响应灵敏（支持触摸滑动手势，平板可兼容鼠标拖拽），渐变动画；
4. 视觉设计：色彩设置适合4-8岁儿童玩耍； 
5. 性能要求：页面加载速度快，滑动无卡顿，动画流畅不掉帧，兼容主流移动浏览器（Chrome、Safari、微信内置浏览器等）。
---
调节这个页面游戏
1. 增加复活按钮，点击复活，可以设置复活次数，默认3次
2. 随着选择的游戏难度变化初始数字，比如选择了2048，初始数字可以适当多出现一些128、256
3. 增加难度选项【16,384、32,768、65,536】，数字增大，初始数字也增加，即，基本没难度，只是显示数字增大了
---
调节前端页面
1. 响应式布局：在手机（320px+宽度）、平板（768px+宽度）上自动适配，弹窗显示区域随屏幕尺寸缩放，按钮不溢出且触摸区域
2. 后续页面还会增加，给我增加一个配置，可以直接在页面上增加需要跳转的页面
---
我的前端页面需要这些音效，我可以从哪里获取？
合并音效	merge.mp3	方块合并时的清脆音效
新方块音效	new-tile.mp3	新方块出现时的轻快音效
移动音效	move.mp3	方块移动时的轻微摩擦音效
游戏结束音效	game-over.mp3	游戏结束时的低沉音效
胜利音效	victory.mp3	获胜时的欢快旋律

---

## 写一个python【上传jar包并运行】的脚本
* 具体需求如下
* 1、可视化操作，jar文件-保存为配置文件，默认：C:\www\gitee\my-blog-api\target\base-service-3.0.0-SNAPSHOT.jar。
* 2、上传到linux服务器，默认“192.168.18.218”，用户名“root”，密码“Yahua3585668”，都保存为配置文件。
* 3、上传目录是：/usr/local/apps/base-service，保存为配置文件。
* 4、上传成功后，运行jar包，确保可以后台运行，并输出运行成功的日志
* 5、配置文件引入方式如下：
    * 绝对路径引入配置文件的代码，确保无论脚本在何处执行，都能准确定位到配置文件
    *  配置在“CONFIG_PATH”的json里面
    * 通用数据库配置：DB_CONFIG_PATH，都是在运行脚本的父级目录的json文件夹，里面的DB_CONFIG.json【脚本用不到本地数据库就不用引入了】
* 7、可视化操作给我加上合适的操作按钮
* 8、记录操作日志“PROCESS_LOG_FILE”


### 配置实例
```python
# 配置与常量
import os
from pathlib import Path

SCRIPT_DIR = Path(os.path.abspath(os.path.dirname(__file__)))
SCRIPT_NAME = "XXXX_XXXX" #脚本名称
CONFIG_DIR = SCRIPT_DIR / "json"
CONFIG_PATH = CONFIG_DIR / f"config_{SCRIPT_NAME}.json"
CONFIG_DIR.mkdir(exist_ok=True)
DB_CONFIG_PATH = (SCRIPT_DIR.parent) / "json" / "DB_CONFIG.json"
PROCESS_LOG_FILE = SCRIPT_DIR / "json" / "logs" / f"log_{SCRIPT_NAME}.log"
```
---

调节前端页面
1. 响应式布局：在手机（320px+宽度）、平板（768px+宽度）上自动适配，弹窗显示区域随屏幕尺寸缩放，按钮不溢出且触摸区域
2. 数字显示的时候，默认固定0.5s往页面上面放1个数字，可以调节，最小可输入0.01
3. 弹窗显示累加的数字，需要全屏显示
---
我接手一个linux服务器【192.168.18.218】，7000端口已经被使用，我需要启用一个新端口7001，访问192.168.18.218：7001，需要打开我部署的页面，例如：/usr/local/apps/base-web/index.html
我需要做哪些配置呢？

---
## 写一个python【上传j基础web页面】的脚本
* 具体需求如下
* 1、可视化操作，选择需要上传的web文件目录-保存为配置文件，默认：G:\CODE\GIT\pcl1350306170.github.io。
  * 可配置目录下不上传的文件或文件夹，默认过滤”.idea“”.git“不上传
* 2、上传到linux服务器，默认“192.168.18.218”，用户名“root”，密码“Yahua3585668”，都保存为配置文件。
* 3、上传目录是：/home/ym_clinic/ym801s/upload/baseweb，保存为配置文件。没有就给我创建
* 4、上传成功后，默认浏览器打开“http://192.168.18.218:7000/upload/baseweb/num/index.html”
* 5、配置文件引入方式如下：
  * 绝对路径引入配置文件的代码，确保无论脚本在何处执行，都能准确定位到配置文件
  *  配置在“CONFIG_PATH”的json里面
  * 通用数据库配置：DB_CONFIG_PATH，都是在运行脚本的父级目录的json文件夹，里面的DB_CONFIG.json【脚本用不到本地数据库就不用引入了】
* 7、可视化操作给我加上合适的操作按钮
* 8、记录操作日志“PROCESS_LOG_FILE”
* 


### 配置实例
```python
# 配置与常量
import os
from pathlib import Path

SCRIPT_DIR = Path(os.path.abspath(os.path.dirname(__file__)))
SCRIPT_NAME = "XXXX_XXXX" #脚本名称
CONFIG_DIR = SCRIPT_DIR / "json"
CONFIG_PATH = CONFIG_DIR / f"config_{SCRIPT_NAME}.json"
CONFIG_DIR.mkdir(exist_ok=True)
DB_CONFIG_PATH = (SCRIPT_DIR.parent) / "json" / "DB_CONFIG.json"
PROCESS_LOG_FILE = SCRIPT_DIR / "json" / "logs" / f"log_{SCRIPT_NAME}.log"
```



---
## 写一个python【获取reddit某个子版块所有信息】的脚本
* 具体需求如下
* 1、可视化操作，选择保存的目录，默认G:\下载\Reddit\java，没有目录就创建，写入配置。
* 2、输入需要爬取的子版块名称，例如“r/java”，写入配置。
* 3、可以选择爬取哪些信息，比如视频、gif、图片、文档等，写入配置，如果需要写入数据库，则使用数据库配置"DB_CONFIG_PATH"。

* 4、可以选择爬取时间段内的帖子，写入配置。
* 分批保存，比如每 50 条写入一次，写入配置。
* 多线程爬取，默认5，写入配置
* 5、配置文件引入方式如下：
  * 绝对路径引入配置文件的代码，确保无论脚本在何处执行，都能准确定位到配置文件
  *  配置在“CONFIG_PATH”的json里面
  * 通用数据库配置：DB_CONFIG_PATH，都是在运行脚本的父级目录的json文件夹，里面的DB_CONFIG.json【脚本用不到本地数据库就不用引入了】
* 7、可视化操作给我加上合适的操作按钮
* 8、记录操作日志“PROCESS_LOG_FILE”
* 还需要我提供什么内容吗


### 配置实例
```python
# 配置与常量
import os
from pathlib import Path

SCRIPT_DIR = Path(os.path.abspath(os.path.dirname(__file__)))
SCRIPT_NAME = "XXXX_XXXX" #脚本名称
CONFIG_DIR = SCRIPT_DIR / "json"
CONFIG_PATH = CONFIG_DIR / f"config_{SCRIPT_NAME}.json"
CONFIG_DIR.mkdir(exist_ok=True)
DB_CONFIG_PATH = (SCRIPT_DIR.parent) / "json" / "DB_CONFIG.json"
PROCESS_LOG_FILE = SCRIPT_DIR / "json" / "logs" / f"log_{SCRIPT_NAME}.log"
```
DB_CONFIG_PATH数据如下：
```
{
  "host": "localhost",
  "port": 3306,
  "user": "root",
  "password": "123456",
  "database": "test",
  "charset": "utf8mb4"
}

```
---


