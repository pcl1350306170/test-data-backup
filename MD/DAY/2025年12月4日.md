2025/12/4
---
明白了，Element Plus的el-table-column没有radio类型。我们可以使用js控制的方式实现单选功能。以下是修改后的代码：



### 主要修改点：

1. **添加了 `singleSelect` 属性**：
   ```javascript
   singleSelect: propTypes.bool.def(false)  // 默认为false，即多选模式
   ```

2. **单选模式下使用自定义el-radio组件**：
    - 使用el-radio组件实现单选效果
    - 通过v-model绑定单选状态

3. **新增单选相关数据**：
    - `paramData.singleSelectionId`：存储单选模式下的选中项ID

4. **修改 `selectChangeEvent` 方法**：
    - 在单选模式下，当用户选择多行时，只保留最后一个选择
    - 自动清除其他行的选中状态

5. **新增 `radioChangeEvent` 方法**：
    - 处理单选模式下点击radio按钮的事件

6. **修改 `setCheckBox` 方法**：
    - 在单选模式下正确回显已选数据

### 使用方法：

- **多选模式（默认）**：
  ```vue
  <YourComponent />
  ```

- **单选模式**：
  ```vue
  <YourComponent :singleSelect="true" />
  ```




现在组件支持通过 `singleSelect` 参数控制是否启用单选模式，默认为多选模式。在单选模式下，用户只能选择一行数据。

---

我理解了问题所在。在单选模式下，虽然使用了`el-radio`组件，但当用户点击时，虽然数据更新了，但界面没有正确显示选中状态。这是因为在单选模式下，我们同时使用了`el-radio`组件和`el-table`的`selection-change`事件，导致状态同步问题。

以下是修改后的代码，确保单选模式下选中状态正确显示：



主要修改点：

1. 在`radioChangeEvent`方法中，当单选模式下点击radio按钮时：
    - 正确更新`paramData.checkedData.list`数组
    - 更新`paramData.singleSelectionId`值
    - 调用`initCheckedList()`刷新右侧列表

2. 在`selectChangeEvent`方法中，当单选模式下通过点击行来选择时：
    - 只保留最后一个选中的元素
    - 同步更新`paramData.singleSelectionId`以确保radio状态正确

3. 在`setCheckBox`方法中，正确处理单选模式下的回显逻辑

这样修改后，单选模式下无论是点击radio按钮还是点击行，都会正确显示选中状态。

---





