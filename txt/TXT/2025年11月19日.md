## 写一个python【快速调用node打包项目】的脚本
* 具体需求如下
* 1、可视化操作
* 2、获取超级管理员权限，自动获取本地nvm工具，获取node版本，进行选择，默认是“14.19.1”，加入配置项
* 3、选取需要打包的项目目录
* 4、有2个打包按钮：build、render2、都打包，点击分别执行
  *  npm run build
  *  npm run lib-render2
  *  点击“”都打包，先执行build，然后执行render2
* 5、实时显示打包进度
* 6、打包完成后自动打开项目目录
*  配置文件引入方式如下：
    * 绝对路径引入配置文件的代码，确保无论脚本在何处执行，都能准确定位到配置文件
    * 通用数据库配置：DB_CONFIG_PATH，都是在运行脚本的父级目录的json文件夹，里面的DB_CONFIG.json
*  可视化操作给我加上合适的操作按钮
*  记录操作日志“PROCESS_LOG_FILE”的log文件里面


### 配置实例
```python
# 配置与常量
import os
from pathlib import Path

SCRIPT_DIR = Path(os.path.abspath(os.path.dirname(__file__)))
SCRIPT_NAME = "chinese_translator"
CONFIG_DIR = SCRIPT_DIR / "json"
CONFIG_PATH = CONFIG_DIR / f"config_{SCRIPT_NAME}.json"
CONFIG_DIR.mkdir(exist_ok=True)
DB_CONFIG_PATH = (SCRIPT_DIR.parent) / "json" / "DB_CONFIG.json"
PROCESS_LOG_FILE = SCRIPT_DIR / "json" / "logs" / f"log_{SCRIPT_NAME}.log"
```
---

* 2025-2132遵义市第一人民医院(桃溪院区)
* 




---
## 写一个python【爬取某个论坛分享的数据】的脚本
* 具体需求如下
* 1、可视化操作
* 2、可以输入网址，例如“https://www.doctors.org/forum.php?mod=forumdisplay&fid=139&page=912”，其中page是这个论坛的页码，可以输入爬取第10页-第100页的数据，点击确定的时候，开始爬取这些页里面所有的文章。网址和爬取页码都加入配置项
* 3、页面爬取规则如下：
  页面内容：
  容器是：<table summary="forum_139" cellspacing="0" cellpadding="0" id="threadlisttableid">
  获取id开头是“normalthread_”的tbody，例如：<tbody id="normalthread_147445">，
  里面存在a标签，获取id以“content_”开头的a标签，例如：
<th class="common">
                <a href="javascript:;" id="content_147445" class="showcontent y" title="更多操作" onclick="CONTENT_TID='147445';CONTENT_ID='normalthread_147445';showMenu({'ctrlid':this.id,'menuid':'content_menu'})"></a>
<em>[<a href="forum.php?mod=forumdisplay&fid=139&filter=typeid&typeid=673">分享</a>]</em>  <a href="thread-147445-1-912.html" onclick="atarget(this)" class="s xst">【防跌倒技巧】作者：kelem</a>

                <img src="static/image/filetype/common.gif" alt="attachment"
                title="附件" align="absmiddle">
                <img src="static/image/common/agree.gif" align="absmiddle" alt="agree" title="帖子被加分">
            </th>
* 4、保存规则
  获取这个a标签的内容和href，这是详情页。调用接口保存到数据库里面：
  接口： http://localhost:28019/api/v1/general-data/batch-save
  参数是一个数组，例如：
  const postData = links.map(link => ({
  dataType: '医疗档案文本录入',
  dataContent: link, // 链接地址
  dataKey: hrefToTextMap.get(link) || link // a标签文本（无文本时用链接兜底）
  }));

前端调用实例是：
$.ajax({
url: `${this.serveApiHost}api/v1/general-data/batch-save`,
type: 'POST',
contentType: 'application/json',
data: JSON.stringify(postData),
dataType: 'json',
success: (response) => {
console.log('链接保存成功', response);
this.handleNextPage(); // 保存成功后处理翻页
},
error: (xhr, status, error) => {
console.error('链接保存失败', error);
console.log('错误状态码：', xhr.status);
}
});

*  配置文件引入方式如下：
    * 绝对路径引入配置文件的代码，确保无论脚本在何处执行，都能准确定位到配置文件
    * 通用数据库配置：DB_CONFIG_PATH，都是在运行脚本的父级目录的json文件夹，里面的DB_CONFIG.json
*  可视化操作给我加上合适的操作按钮
*  记录操作日志“PROCESS_LOG_FILE”的log文件里面


### 配置实例
```python
# 配置与常量
import os
from pathlib import Path

SCRIPT_DIR = Path(os.path.abspath(os.path.dirname(__file__)))
SCRIPT_NAME = "chinese_translator"
CONFIG_DIR = SCRIPT_DIR / "json"
CONFIG_PATH = CONFIG_DIR / f"config_{SCRIPT_NAME}.json"
CONFIG_DIR.mkdir(exist_ok=True)
DB_CONFIG_PATH = (SCRIPT_DIR.parent) / "json" / "DB_CONFIG.json"
PROCESS_LOG_FILE = SCRIPT_DIR / "json" / "logs" / f"log_{SCRIPT_NAME}.log"
```
---
本地设备模拟：

http://localhost:8080/#/iframeIndex?terminalId=19&terminalType=1&previewType=web&sync=2&t=1763523438135

---


