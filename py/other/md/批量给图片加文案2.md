2026/3/9
---

* 使用python写一个关于【批量给图片加文案】的脚本
* 具体需求如下
* 可视化操作，保存为“pyw”
* 可以输入/选择【需要处理图片的图片目录】，获取所有图片，写入配置
* 可以输入【文案字体大小】，默认25px，写入配置
* 可以输入/选择【文案字体颜色】，默认黑色，写入配置
* 可以输入/选择【文案背景颜色】，默认白色，写入配置
* 可以输入/选择【文案字体类型】，默认微软雅黑，写入配置
* 可以输入/选择【文案透明度】，默认1，写入配置
* 可以输入【文案显示区域高度】，默认150px，写入配置
* 可以输入【需要给图片增加的文案】，一般来说是每个图片都要加，所以可以给每个图片配一个输入框，没有输入就跳过，写入配置

功能是在图片的下方，延伸出一块和图片等宽的，高度配置的白色区域，把配置好的文字显示在区域内

* 配置文件引入方式如下：
    * 绝对路径引入配置文件的代码，确保无论脚本在何处执行，都能准确定位到配置文件
    * 配置在“CONFIG_PATH”的json里面
    * 通用数据库配置：DB_CONFIG_PATH，都是在运行脚本的父级目录的json文件夹，里面的DB_CONFIG.json【脚本用不到本地数据库就不用引入了】
* 可视化操作给我加上合适的操作按钮
* 记录操作日志“PROCESS_LOG_FILE”

```python
# 配置与常量
import os
from pathlib import Path

SCRIPT_DIR = Path(os.path.abspath(os.path.dirname(__file__)))
SCRIPT_NAME = "XXXX_XXXX" #脚本名称
CONFIG_DIR = SCRIPT_DIR / "json"
CONFIG_PATH = CONFIG_DIR / f"config_{SCRIPT_NAME}"
CONFIG_DIR.mkdir(exist_ok=True)
DB_CONFIG_PATH = (SCRIPT_DIR.parent) / "json" / "DB_CONFIG"
PROCESS_LOG_FILE = SCRIPT_DIR / "json" / "logs" / f"log_{SCRIPT_NAME}.log"
```

DB_CONFIG_PATH数据如下：

```
{
  "host": "localhost",
  "port": 3306,
  "user": "root",
  "password": "123456",
  "database": "test",
  "charset": "utf8mb4"
}

```
* 2026年3月9日10:03:10
* 增加配置方式：是否在右下角开一块空白区域显示文案【是/否】
* 如果选是，则直接在图片右下角覆盖一块空白，把文案放上去
* 可以配置空白区域的宽和高，默认宽为“10%”，高为“15%”
* 如果文案较长，可以适当撑开空白区域，可以换行
* 现在脚本图片输入文案区域，鼠标滚轮无响应，给我处理一下
* 给我提供全部代码
