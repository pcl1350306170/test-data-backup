2025/12/4
---

* 使用python写一个关于【超长PDF裁剪拼接为图片】的脚本
* 具体需求如下
* 可视化操作，保存为“pyw”
* 可以选择一个PDF文件【可能超过100页】，一般是表格或者纯文字
* 先转为一整个图片，默认背景色是黑色，字体色是红色，可配置
* 处理这个高度很大的图片
* 可以输入按照高度均分为几份，默认3，写入配置
* 裁剪出来的图片，从左到右拼接起来。目标是获得一个没那么高，宽度增加的图片
* 可以选择输出目录，默认脚本所在目录
* 配置文件引入方式如下：
    * 绝对路径引入配置文件的代码，确保无论脚本在何处执行，都能准确定位到配置文件
    * 配置在“CONFIG_PATH”的json里面

* 可视化操作给我加上合适的操作按钮
* 记录操作日志“PROCESS_LOG_FILE”

```python
# 配置与常量
import os
from pathlib import Path

SCRIPT_DIR = Path(os.path.abspath(os.path.dirname(__file__)))
SCRIPT_NAME = "XXXX_XXXX" #脚本名称
CONFIG_DIR = SCRIPT_DIR / "json"
CONFIG_PATH = CONFIG_DIR / f"config_{SCRIPT_NAME}.json"
CONFIG_DIR.mkdir(exist_ok=True)
DB_CONFIG_PATH = (SCRIPT_DIR.parent) / "json" / "DB_CONFIG.json"
PROCESS_LOG_FILE = SCRIPT_DIR / "json" / "logs" / f"log_{SCRIPT_NAME}.log"
```

DB_CONFIG_PATH数据如下：

```
{
  "host": "localhost",
  "port": 3306,
  "user": "root",
  "password": "123456",
  "database": "test",
  "charset": "utf8mb4"
}

```
