2025/12/16
---

* 使用python写一个关于【调用接口批量生成测试数据】的脚本
* 具体需求如下
* 可视化操作，保存为“pyw”
* 可以输入【需要调用接口地址】，例如：http://192.168.18.228:7000/clinic/api/qcss/register/made，写入配置
* 可以输入【请求方式】，例如：【post】，写入配置
* 可以输入【请求参数】，例如：【{"hasBook":1,"registerTime":"2025-12-16 09:01:36","signTime":"2025-12-16 09:01:36","patientName":"张三","patientIdNo":"zs1","sex":"1","age":0,"ageType":"2","hasPay":0,"registerObjId":1,"orderTags":""}】，写入配置
* 可以输入【请求参数哪个字段需要替换为真实数据】，例如：【patientName替换为真实人名（随机生成），patientIdNo替换为6位随机数】，写入配置
* 可以输入【headers信息】，例如：【POST /clinic/api/qcss/register/made HTTP/1.1
  Accept: application/json, text/plain, */*
  Accept-Encoding: gzip, deflate
  Accept-Language: zh-CN,zh;q=0.9,en;q=0.8,ja;q=0.7
  Authorization: Bearer eyJ0eXAiOiJKV1QiLCJhbGciOiJIUzI1NiJ9.eyJsb2dpblR5cGUiOiJsb2dpbiIsImxvZ2luSWQiOiJzeXNfdXNlcjoxOm51bGwiLCJyblN0ciI6InhoWXRFRzdSZ01wZXdFTzJDdlU3cnVaZFJjejZ2MUl0IiwiY2xpZW50aWQiOiJlNWNkN2U0ODkxYmY5NWQxZDE5MjA2Y2UyNGE3YjMyZSIsInRlbmFudElkIjoiMDAwMDAwIiwidXNlcklkIjoxfQ.ZgKqt_tvTjhhIzFr812u75923CQGLX9fgDz3SCKyTuM
  Connection: keep-alive
  Content-Language: zh_CN
  Content-Length: 202
  Content-Type: application/json;charset=UTF-8
  Cookie: tenantId=000000; username=yarward; password=Yahua3585668; rememberMe=true; perf_dv6Tr4n=1
  Host: 192.168.18.228:7000
  Origin: http://192.168.18.228:7000
  Referer: http://192.168.18.228:7000/
  User-Agent: Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/143.0.0.0 Safari/537.36
  clientid: e5cd7e4891bf95d1d19206ce24a7b32e】，写入配置
* 可以输入【调用几次接口】，例如：【10】，则调用10次接口生成10条数据，写入配置
* 点击开始按钮，调用接口开始生成数据
* 配置文件引入方式如下：
    * 绝对路径引入配置文件的代码，确保无论脚本在何处执行，都能准确定位到配置文件
    *  配置在“CONFIG_PATH”的json里面
    * 通用数据库配置：DB_CONFIG_PATH，都是在运行脚本的父级目录的json文件夹，里面的DB_CONFIG.json【脚本用不到本地数据库就不用引入了】
* 可视化操作给我加上合适的操作按钮
* 记录操作日志“PROCESS_LOG_FILE”


```python
# 配置与常量
import os
from pathlib import Path

SCRIPT_DIR = Path(os.path.abspath(os.path.dirname(__file__)))
SCRIPT_NAME = "XXXX_XXXX" #脚本名称
CONFIG_DIR = SCRIPT_DIR / "json"
CONFIG_PATH = CONFIG_DIR / f"config_{SCRIPT_NAME}"
CONFIG_DIR.mkdir(exist_ok=True)
DB_CONFIG_PATH = (SCRIPT_DIR.parent) / "json" / "DB_CONFIG"
PROCESS_LOG_FILE = SCRIPT_DIR / "json" / "logs" / f"log_{SCRIPT_NAME}.log"
```
DB_CONFIG_PATH数据如下：
```
{
  "host": "localhost",
  "port": 3306,
  "user": "root",
  "password": "123456",
  "database": "test",
  "charset": "utf8mb4"
}

```

---


* 完善这个脚本：
* 不用header参数了，改为输入cookie，你给拼接到header里面去请求接口
* 姓名从姓名库里面获取生成，姓名库如下：
* 在【(SCRIPT_DIR.parent) / "json" 】 目录下有2个json文件，
* LastName.json【姓】: 格式是：["观博","欣竹","欣阳"]
* FirstName.json【名】: 格式是：["博","欣","阳"]


写一个新脚本，用来快速生成添加模板的sql：
1、可视化，保存配置，记录日志
2、输出一个“床头卡脚本.sql”的文件，可以选择输出目录
3、sql基本内容如下：
// 修改字段的类型
ALTER TABLE `wn_bedHeadConfig`
MODIFY COLUMN `image` LONGTEXT;

// 简易模板：
INSERT INTO `YHDB`.`wn_bedHeadConfig` (`id`, `label`, `path`, `image`, `type`, `createUser`, `createTime`, `updateUser`, `updateTime`, `isDelete`, `isEnable`) VALUES ('nURf4pB45613ESYv', 'M1-1', '/a10/bedhead/bedSideCard2-1.html', '***', '床头分机-1024*600', '703c7d9bcc874fdbb3eaacf153650eca', '2021-06-21 08:17:40', '703c7d9bcc874fdbb3eaacf153650eca', '2021-11-10 15:33:52', '0');


4、具体sql字段内容如下：
id： 生成一个uuid
label： 手动输入一个模板名称
path： 手动输入文件路径
image： 上传或者粘贴一个图片，自动压缩到30k以内，并转为base64格式
type： 下拉选“床头分机-1024*600”“床旁分机-1920*1080”，或者手动输入
createUser： 空着
createTime： 当前时间
updateUser： 空着
updateTime： 当前时间

5、点击开始，验证都输入正确，直接给我生成一个sql脚本文件
